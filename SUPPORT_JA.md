# SwiftRename — サポート

SwiftRenameは、ファイルの名前変更・コピー・移動を一括で行うmacOSアプリです。写真やビデオは撮影日（EXIF）に基づいて名前が付けられ、ご希望に応じて年・月・日ごとのフォルダに自動的に整理されます。また、独自のプレフィックスを付けて連番を振ったり、名前をそのまま保持してフォルダへの整理のみを行うこともできます。重複ファイルはサイズと内容に基づいて検出され、スキップされます。

## アプリ内のヘルプ

SwiftRenameには完全なヘルプが内蔵されています。「ヘルプ」メニューから、または⌘/（コマンド＋スラッシュ）で開けます。すべての名前変更モード、フォルダ構造、重複検出、その他のオプションについて説明しています。

## よくある質問

**写真に撮影日が付きません。**
「カメラの日付を使用」が有効になっているかご確認ください。ファイルにEXIF撮影日がない場合、SwiftRenameはそのファイルの作成日を使用します。

**一部のファイルがスキップされたのはなぜですか？**
おそらく重複検出が、それらのファイルをすでに存在するもの（出力フォルダ内に同じサイズ・同じ内容のファイルがある）と判定したためです。ダイアログには両方のファイルが並べて比較表示されます。このチェックは「重複をチェック」で無効にすることもできます。

**名前変更を取り消すことはできますか？**
はい — 実行直後に⌘Z（編集 → 取り消す）で取り消せます。コピーされたファイルはゴミ箱に移動し、移動されたファイルは元の場所に戻ります。

**アプリの表示言語を変えるには？**
macOSではアプリごとに言語を設定できます：「システム設定 → 一般 → 言語と地域」の「アプリ」セクションで、SwiftRenameを追加して、ご希望の言語を選択してください。

## お問い合わせ

SwiftAppsBavaria — SwiftAppsBavaria@gmx.net

問題をお知らせいただく際は、選択したモード、フォルダ構造、実行した操作（コピー／移動）を簡単にご記載ください — 原因の特定が大幅に容易になります。
